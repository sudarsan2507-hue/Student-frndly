# Dependencies
node_modules/

# Environment variables
.env
.env.local

# Build outputs
dist/
build/

# Logs
*.log
logs/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Storage
backend/data/*.json
!backend/data/.gitkeep

# Claude Code skills (cloned from upstream repos, not tracked)
.claude/skills/

# Raw reference art — a downloaded image of unknown licence, kept local.
# The *processed* derivatives in frontend/src/assets/ (colorized cutout,
# voxel grid, webp) are committed: Login/Signup import them directly, and
# a build on a clean checkout (Vercel) fails to resolve those imports
# without them actually being in the repo.
/Reference/
